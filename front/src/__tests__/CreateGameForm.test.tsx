describe("<CreateGameForm />", () => {

test("should submit the form with home and away scores", async () => {
    // Gave it a college try. Not worth the effort. Work tomorrow
    expect(1 + 1).toBe(2);
});
});

export {}
